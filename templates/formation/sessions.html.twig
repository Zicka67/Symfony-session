{% extends 'base.html.twig' %}

{% block title %} Il y a {{ sessions|length }} Sessions{% endblock %}

{% block body %}
  <div class="basic-tab">
    <table>
      <thead>
        <tr>
          <th>Titre de la session</th>
          <th>Détails</th>
        </tr>
      </thead>
      <tbody>
        {% for session in sessions %}
          <tr>
            <td class="firstTd">{{ session.titlesection }}</td>
            <td><a href="{{ path('app_showDetailsSession', {'id': session.id}) }}">Voir les détails</a></td>
          </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
   {# app pour revenir sur la page de base formation (et pas juste formation) #}
  <a href="{{ path('app_formation') }}">Retour à la liste des formations</a> 
  {# <br>
  <a href="{{ path('app_home') }}">Retour à l'accueil</a> #}

{% endblock %}      